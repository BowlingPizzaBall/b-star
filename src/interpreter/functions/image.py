from typing import List
from src.interpreter.expression import Expression

def image(block: List, codebase):
    if block[1:9] = 'https://':
        return Expression(block[1], codebase)
    else:
        print("Uhhh, kid? I ain't taking care of that image if it's not a link.")
        return None